#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_ng-annotate@1.2.2@ng-annotate/build/es5/ng-annotate" "$@"
  ret=$?
else 
  node  "$basedir/../_ng-annotate@1.2.2@ng-annotate/build/es5/ng-annotate" "$@"
  ret=$?
fi
exit $ret
